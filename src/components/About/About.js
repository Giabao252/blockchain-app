import React from "react";
import useStyles from './styles';
import {Typography} from '@material-ui/core'

const About = () => {
    const classes = useStyles();

    return (
        <div>
            <Typography variant="h1"> How to center a div in another div??</Typography>
        </div>
    )
}